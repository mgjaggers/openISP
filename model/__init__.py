"""Compatibility package for the original openISP algorithm implementations.

New code should use :mod:`openisp.stages`.  These modules remain importable so
existing users of ``model.*`` are not broken while the algorithms are migrated.
"""
